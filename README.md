PHP Sentinel
============

Framework-agnostic authorization core for PHP applications.

## Development

Install dependencies:

```sh
make composer-install
```

Run tests:

```sh
make test
```

> PHPUnit is configured, but the `tests/` directory has been intentionally left untouched.
